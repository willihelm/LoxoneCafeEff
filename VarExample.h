// Loxone Daten
#define LOXONE_URL "127.0.0.1"
#define LOXONE_PORT 80
#define LOXONE_USER "admin"
#define LOXONE_PASSWORD "admin"

// Anzahl der Buttons
#define BUTTON_COUNT 12

// Ab diesem Pin werden immer 2 pro Button verwendet
// (1. Pin LED[weisses Kabel], 2. Pin Input Taster[rotes Kabel])
#define START_PIN 16

// Mac Adresse des Arduinos, kann jede beliebige sein, 
// solange im Netzwerk einzigartig
#define MAC_ADDRESS "{ 0xDE, 0xAD, 0xBE, 0xEF, 0xFE, 0xED }"