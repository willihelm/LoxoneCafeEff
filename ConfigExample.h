// Vorlage fuer die Datei "Var.h"
// Diese Angaben werden im Programm verwendet

// Loxone Daten
#define LOXONE_URL "192.168.1.2"
#define LOXONE_PORT 80
#define LOXONE_USER "username"
#define LOXONE_PASSWORD "password"

// Mac Adresse des Arduinos, kann jede beliebige sein, 
// solange im Netzwerk einzigartig
#define MAC_ADDRESS "{ 0xDE, 0xAD, 0xBE, 0xEF, 0xFE, 0xED }"

// IP Adresse, wird verwendet, wenn keine IP vom DHCP Server bezogen wurde
#define FALLBACK_IP 192,168,1,1
